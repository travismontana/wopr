---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: wopr-config-db-cluster
spec:
  instances: 1
  storage:
    size: 1Gi
  enableSuperuserAccess: true
  bootstrap:
    initdb:
      database: app
      owner: app