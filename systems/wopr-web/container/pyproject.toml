[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wopr-web"
version = "0.1.0"
dependencies = [
    # Your requirements go here, or keep requirements.txt
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]