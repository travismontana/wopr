# Core web API
fastapi~=0.115.6
uvicorn[standard]~=0.34.0
picamera2
git+https://github.com/travismontana/wopr.git#subdirectory=pymods/wopr-core
# Data validation (FastAPI depends on pydantic, but we pin explicitly)
pydantic~=2.10.4
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0

# Camera capture
opencv-python-headless~=4.10.0.84

# If your existing code already depends on these, keep them here:
# (wopr.config / yaml / etc.)
PyYAML~=6.0.2