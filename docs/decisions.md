1) “Login with Google” (OAuth/OIDC)
Google is the global identity provider, not “your company Google.”
Anyone can sign up
No per-user licensing
Great for public apps / hobby apps

project_status = {
    "idea" = "Not started yet",
    "staged" = "Have game, havent started human learning",
    "pretraining" = "Have game, learning, havent started training",
    "training" = "Training the model",
    "trained" = "Model has been trained"
}

project_state = {
    "open" = "Live project",
    "archived" = "Archived project"
}

projects = {
    "duneimpup" = {
        "id" = id,
        "uuid" = "UUID",
        "name" = "Dune Imperium Uprising",
        "description" = "Dune game",
        "notes" = "",
        "project_status" = "pretraining",
        "project_state" = "open",
        "project_created_datetime" = "datetime",
        "others"
    }
}