apiVersion: v1
kind: Secret
metadata:
  name: {{ include "wopr-config.fullname" . }}-db-secret
  labels:
    {{- include "wopr-config.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.postgresql.password | b64enc | quote }}
